syntax = "proto3";

package auth;

service AuthService {
  rpc GetAllUsers (Empty) returns (UserList);
}

message Empty {}

message User {
  string id = 1;
  string name = 2;
}

message UserList {
  repeated User users = 1;
}
